version: '3.8'

services:
  vidsrc-api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - NODE_ENV=production
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
